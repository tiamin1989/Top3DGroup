import './index.css';

/* const dropdowns = document.querySelectorAll('li.header__dropdown-content');
const dropdownClick = (evt) => {
  if (evt.target.classList.contains('header__dropdown-content'))
  evt.target.querySelector('ul.header__list').classList.remove('header__list_hidden');
};

dropdowns.forEach((dropdown) => {
  dropdown.addEventListener('click', dropdownClick);
});
 */
